datasource db {
  provider = "sqlite" // or "postgresql", "mysql", etc.
  url      = env("DATABASE_URL")
}

model User {
  id       Int     @id @default(autoincrement())
  email    String  @unique
  password String
}